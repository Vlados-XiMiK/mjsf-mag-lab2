{
  "name": "tymoshchuk",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "docker-run": "docker build --tag tymoshchuk:latest . && docker run --rm -p 8080:80 --name tymoshchuk-instance tymoshchuk:latest",
    "docker-stop": "docker stop tymoshchuk-instance"
  },
  "dependencies": {
    "vue": "^3.4.21"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "vite": "^5.2.0"
  }
}
